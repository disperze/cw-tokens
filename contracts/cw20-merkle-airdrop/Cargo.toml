[package]
name = "cw20-merkle-airdrop"
version = "0.14.3"
authors = ["Orkun Kulce <orkunkl@users.noreply.github.com>", "Terraform Labs, PTE."]
edition = "2018"
description = "An Airdrop contract for allowing users to claim rewards with Merkle Tree based proof"
license = "Apache-2.0"
repository = "https://github.com/CosmWasm/cw-tokens"
homepage = "https://cosmwasm.com"
documentation = "https://docs.cosmwasm.com"

exclude = [
  "contract.wasm",
  "hash.txt",
]

[lib]
crate-type = ["cdylib", "rlib"]

[features]
library = []

[dependencies]
cw-utils = "2.0"
cw2 = "2.0"
cosmwasm-std = { version = "2.2", features = ["cosmwasm_2_1"] }
cw-storage-plus = "2.0"
serde = { version = "1.0.137", default-features = false, features = ["derive"] }
thiserror = "1.0.31"
hex = "0.4"
rlp = "0.5"
sha2 = "0.10"
sha3 = "0.10"
cosmwasm-schema = "2.2"
semver = "1.0.14"
[dev-dependencies]
cw-multi-test = "2.2"
